<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net461</TargetFramework>
    <CodeAnalysisRuleSet>ValidCode.ruleset</CodeAnalysisRuleSet>
    <Description>A project only used by the HasppyPathTests. It contains all kinds of weird variations that is styill valid code. We use it to test all analyzers. If a bug is found we extract it to a minimal repro and use it in a test for the analyzer but we leave this project as is.</Description>
  </PropertyGroup>

  <Import Project="..\.paket\Paket.Restore.targets" />

  <ItemGroup>
    <ProjectReference Include="..\IDisposableAnnotations\IDisposableAnnotations.csproj" />
  </ItemGroup>
</Project>